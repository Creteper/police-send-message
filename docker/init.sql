-- 警察-村长违章消息系统 数据库初始化脚本
-- 注意：TypeORM会自动创建表结构，此脚本仅用于添加初始数据

SET NAMES utf8mb4;
SET CHARACTER SET utf8mb4;

-- 数据库字符集设置
ALTER DATABASE police_message CHARACTER SET = utf8mb4 COLLATE = utf8mb4_unicode_ci;

-- =====================================================
-- 以下是五大连池市村庄数据示例
-- 您可以根据实际情况修改或添加村庄数据
-- =====================================================

-- 村庄数据将通过应用程序的种子脚本自动创建
-- 如果您需要手动添加更多村庄，可以使用以下格式：
-- INSERT INTO villages (name, area, created_at) VALUES ('村庄名', '所属区域', NOW());

-- =====================================================
-- 初始配置数据
-- =====================================================

-- 系统配置也将通过种子脚本自动创建
-- 如果您需要修改超时时间，可以执行：
-- UPDATE system_configs SET value = '48' WHERE `key` = 'unread_timeout_hours';
-- 上面的命令将超时时间改为48小时

-- =====================================================
-- 用户账号
-- 默认账号将通过种子脚本自动创建：
-- 警察账号: police001 / 123456
-- 村长账号: chief001~chief010 / 123456
-- =====================================================

-- 如果您需要手动添加警察账号，密码需要使用bcrypt加密
-- 下面是密码 '123456' 的bcrypt哈希值示例（实际使用时请重新生成）
-- $2a$10$example.hash.here

-- 手动添加村庄的示例（取消注释后可使用）：

INSERT INTO villages (name, area, created_at) VALUES
('青山村', '五大连池市青山镇', NOW()),
('红旗村', '五大连池市红旗乡', NOW()),
('永安村', '五大连池市永安镇', NOW());

